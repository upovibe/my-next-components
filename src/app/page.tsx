const Homepage = () => {
  return (
    <div>
      <div class="bg-primary dark:bg-crimson">
        <p class="text-soft dark:muted">This is a subheading</p>
        <button class="bg-accent dark: text-white dark:text-muted">
          Click Me
        </button>
      </div>
    </div>
  );
};

export default Homepage;